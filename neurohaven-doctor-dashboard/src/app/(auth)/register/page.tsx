import RegisterForm from "@/components/auth/RegisterForm";

export const metadata = {
  title: "Clinical Portal Registration — NeuroHaven",
  description: "Register a clinical workspace account on the NeuroHaven care workstation.",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
